# Secrets / credentials — never commit
.env
.env.*
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.*
*.keystore
credentials.json
secrets.json

# Local tooling / editor config
.claude/
.vscode/
.idea/
.DS_Store
.mcp.json
skills-lock.json

# Local preview / scratch artifacts (never deploy)
.playwright-mcp/
hero-*.png
cards*.png
about-services.png
full-desktop.png

# Dependencies / build output
node_modules/
dist/
build/
